{
  "name": "arogyamind-backend",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "install-deps": "pip install -r requirements.txt",
    "build": "npm install && pip install -r requirements.txt"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  }
}